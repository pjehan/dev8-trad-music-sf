<form method="post" action="{{ path('app_pub_delete', {'id': pub.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pub.id) }}">
    <button class="btn">Delete</button>
</form>
